# GitHub OAuth Configuration
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Set Authorization callback URL to: http://localhost:3001/api/auth/github/callback
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# Session Configuration
# Generate a strong random string (minimum 32 characters)
SESSION_SECRET=your_super_secret_session_key_minimum_32_characters_long

# Email Configuration (Gmail SMTP for verification codes)
# Use Gmail App Password: https://support.google.com/mail/answer/185833
EMAIL_USER=your_gmail_address@gmail.com
EMAIL_PASS=your_gmail_app_password_here
EMAIL_FROM=Printmeister HU <your_gmail_address@gmail.com>

# Database
DATABASE_PATH=./database/farm.db

# OctoPrint API Keys (REQUIRED for printer control and monitoring)
# These are generated from each OctoPrint web interface:
# 1. Access each OctoPrint at http://3dprinters:5001, :5002, :5003
# 2. Go to Settings → API → Generate new API key
# 3. Copy each key to the corresponding variable below
OCTOPRINT1_API_KEY=your_octoprint1_api_key_here
OCTOPRINT2_API_KEY=your_octoprint2_api_key_here
OCTOPRINT3_API_KEY=your_octoprint3_api_key_here

# Application URLs
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:3001

# GitHub OAuth App Setup Instructions:
# 1. Go to GitHub → Settings → Developer settings → OAuth Apps
# 2. Click "New OAuth App"
# 3. Application name: "Printmeister HU"
# 4. Homepage URL: http://localhost:3000 (or your production URL)
# 5. Authorization callback URL: http://localhost:3001/api/auth/github/callback
# 6. Copy Client ID and Client Secret to the variables above
#
# Gmail App Password Setup Instructions:
# 1. Go to Google Account → Security → 2-Step Verification (enable if not already)
# 2. Go to App passwords → Generate app password for "Mail"
# 3. Use the generated password in EMAIL_PASS above
#
# User Requirements for GitHub OAuth:
# - Users must have their HU email (@hu.nl or @student.hu.nl) added to their GitHub account
# - The HU email must be verified in GitHub
# - The email verification flow creates accounts that can then be linked to GitHub
